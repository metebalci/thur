# Monte Carlo regression seed corpus — thurvtl
#
# One seed per line (decimal, optionally followed by "# note"). Blank
# lines and lines starting with '#' are ignored. The nightly workflow
# (.github/workflows/nightly-monte-carlo-vtl.yml) replays every seed
# here with --quick before drawing a fresh random seed, so a seed that
# ever caught a bug becomes a permanent regression guard — this is the
# mechanism behind the "grows the seed corpus over time" claim.
#
# To add a seed: a failed run prints "Re-run with: --seed N". Add that N
# here with a one-line note; once the bug is fixed the replay pins the
# fix forever. Replay it locally with:
#   ./vtl/scripts/test-monte-carlo.sh --seed N --quick

7728896004901786904   # 2026-06-09 known-good smoke; first green run after the encrypted-chunk verify fix (issue surfaced by embedding encryption)
